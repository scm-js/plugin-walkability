{
  "name": "scmjs-plugin-walkability",
  "version": "1.1.1",
  "private": true,
  "description": "Walkability — a plugin for the scmJS StarCraft map editor.",
  "author": "Jeany",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "esbuild plugin.ts --bundle --format=esm --target=es2022 --platform=browser --outfile=dist/plugin.js --banner:js=\"// Generated by npm run build - do not edit; the source is plugin.ts.\"",
    "dev": "npm run build -- --watch",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@scm-js/plugin-api": "github:scm-js/plugin-api",
    "esbuild": "^0.28.2",
    "typescript": "~5.9.2",
    "vitest": "^3.2.4"
  }
}
